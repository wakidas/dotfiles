gui:
  language: en
customCommands:
  - key: "C"
    context: "files"
    description: "Codex でコミットメッセージを生成"
    command: 'bash "$(dirname "$(realpath "$HOME/Library/Application Support/lazygit/config.yml")")/agent-commit.sh"'
    output: terminal
